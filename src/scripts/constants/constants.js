import { SELECTORS } from './index.js'

const cityCardBlock = document.querySelector(SELECTORS.cityCardBlock)
const weatherBlock = document.querySelector(SELECTORS.weatherBlock)
const forecastCardsBlock = document.querySelector(SELECTORS.forecastCardsBlock)
const searchInput = document.querySelector(SELECTORS.searchInput)
const searchButton = document.querySelector(SELECTORS.searchButton)
const sliderBlock = document.querySelector(SELECTORS.sliderBlock)

export {cityCardBlock, weatherBlock, forecastCardsBlock, searchInput, searchButton,sliderBlock}


